import React from "react";
import Home from "../../layouts/home"; 

const Assistants: React.FC = () => {
    return (
        <Home>
            <div>
                <h1 className="text-3xl font-bold text-white mb-4">Assistants</h1>
            </div>
        </Home>
    );
};

export default Assistants;